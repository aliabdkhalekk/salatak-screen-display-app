import 'settings_controller.dart';

export 'settings_controller.dart'
    show
        SettingsController,
        settingsControllerProvider,
        settingsRepositoryProvider;

typedef SettingsService = SettingsController;
